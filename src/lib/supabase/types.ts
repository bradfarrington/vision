// Supabase generated types.
//
// Placeholder until the schema is generated from the live DB. AFTER you have
// pushed both migration waves (`npx supabase db push`), regenerate real types:
//
//   npx supabase gen types typescript --linked > src/lib/supabase/types.ts
//
// That replaces this file with fully-typed tables, so every query the app makes
// is checked against the actual schema.
//
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
